
<% if (page.posts.length > 0) { %>
  <div id="recent-posts">
    <h1>Recent Posts</h1>
    <% page.posts.sort('date', -1).limit(10).each(function(post) { %>
      <div class="recent-post-item">
        <h2>
          <a href="<%=config.root + post.path%>"><%=post.title%></a>
          <% if (post.date) { %>
            <time datetime="<%=post.date.toJSON()%>" class="text-muted">
              <%=date(post.date)%>
            </time>
          <% } %>
        </h2>
      </div>
    <% }); %>
  </div>
<% } %>
